# Launcher icon source

Drop the final BeautyHub Vendor icon here as `app_icon.png`
(1024x1024, square, no transparency for iOS), then run:

    dart run flutter_launcher_icons

That regenerates all Android mipmaps and the iOS AppIcon set.
Until then, the app still ships the default Flutter icon.
